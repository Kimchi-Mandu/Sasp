<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>파일 업로드</title>
  <style>
    body {
      font-family: sans-serif;
      padding: 40px;
      background: #000;
      color: #0f0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      position: relative;
    }
    .top-button {
      position: absolute;
      top: 20px;
      left: 20px;
    }
    .top-button a {
      color: #0f0;
      text-decoration: none;
      font-size: 24px;
    }
    .top-button a:hover {
      color: #fff;
    }
    h1 {
      margin-bottom: 30px;
      text-align: center;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
      width: 100%;
      max-width: 400px;
    }
    input[type="file"] {
      padding: 10px;
      background: #000;
      color: #0f0;
      border: 1px solid #fff;
      border-radius: 5px;
    }
    button {
      padding: 10px;
      background: #000;
      color: #0f0;
      border: 1px solid #fff;
      border-radius: 5px;
      cursor: pointer;
      transition: background 0.3s;
    }
    button:hover {
      background: #111;
    }
    p.notice {
      margin-top: 20px;
      font-size: 14px;
      color: #ccc;
    }
  </style>
</head>
<body>
  <div class="top-button">
    <a href="/">🏠</a>
  </div>

  <h1>파일 업로드</h1>

  <% if (user) { %>
    <!-- 로그인한 경우에만 업로드 폼 보여주기 -->
    <form method="POST" action="/upload" enctype="multipart/form-data">
      <input type="file" name="file" required />
      <button type="submit">업로드</button>
    </form>
    <p class="notice">※ Webshell, HTML 파일 등 실습용 파일 업로드 가능</p>
  <% } else { %>
    <!-- 로그인 안 된 경우 -->
    <p>※ 파일 업로드를 하려면 <a href="/login" style="color:#0f0;">로그인</a>이 필요합니다.</p>
  <% } %>

</body>
</html>
